import React from 'react';
import { ActivityIndicator } from 'antd-mobile';

export default () => (
	<div className="toast-example">
		<ActivityIndicator
			toast
			text="Loading..."
		/>
	</div>
);
